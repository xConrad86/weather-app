import { IPlace } from '../interface/types';
import { Place } from './Place';

export const cities: IPlace[] = [
  new Place('London', 'uk'),
  new Place('Berlin', 'de'),
  new Place('Warsaw'),
  new Place('Rome', 'it'),
];
